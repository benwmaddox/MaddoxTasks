interface:
  display_name: "Maddox Tasks"
  short_description: "Run Maddox Tasks in agent JSON mode for task retrieval and updates"
  default_prompt: "Use Maddox Tasks agent commands only: read with 'agent issues' and mutate with 'agent command'; for description/comment commands set actor to the exact model id (e.g. gpt-5.2 or claude-sonnet)."
