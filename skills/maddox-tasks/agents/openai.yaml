interface:
  display_name: "Maddox Tasks"
  short_description: "Run Maddox Tasks in agent JSON mode for task retrieval and updates"
  default_prompt: "Use Maddox Tasks agent commands only: read with 'agent issues' (or 'agent next' for next-task selection) and mutate with 'agent command'; for description/comment commands set actor to the exact model id (in payload or via --actor)."
